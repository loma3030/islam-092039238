import 'package:flutter/material.dart';

class Regularity extends StatelessWidget {
  const Regularity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
